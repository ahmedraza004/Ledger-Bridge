{
  "name": "@ledgerbridge/infra-postgres",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@ledgerbridge/domain": "*",
    "@ledgerbridge/shared": "*",
    "@prisma/client": "^6.3.0",
    "prisma": "^6.3.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
